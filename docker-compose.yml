version: '3.8'

services:
  wavelane:
    container_name: wavelane
    build: .
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    restart: always