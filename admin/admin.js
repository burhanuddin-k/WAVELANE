const f=document.querySelector("#form"),m=document.querySelector("#msg"),cat=document.querySelector("#catalog"),st=document.querySelector("#stats");
const esc=x=>String(x??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));const cv=s=>s.cover_url||"/assets/default-cover.svg";
async function stats(){let x=await fetch("/api/stats").then(r=>r.json());st.innerHTML=`<div class="stat"><span>Total songs</span><strong>${x.songs}</strong></div><div class="stat"><span>Published</span><strong>${x.published}</strong></div><div class="stat"><span>Total plays</span><strong>${x.plays}</strong></div><div class="stat"><span>Artists</span><strong>${x.artists}</strong></div>`}
async function load(){let a=await fetch("/api/admin/songs").then(r=>r.json());cat.innerHTML=a.length?a.map(s=>`<div class="row"><img src="${esc(cv(s))}"><div><b>${esc(s.title)}</b><small>${esc(s.artist)} · ${esc(s.album)}</small></div><div class="plays">${s.plays} plays</div><button class="del" data-id="${s.id}">Delete</button></div>`).join(""):"<p>No songs uploaded yet.</p>";document.querySelectorAll(".del").forEach(b=>b.onclick=async()=>{if(confirm("Delete this song?")){await fetch("/api/admin/songs/"+b.dataset.id,{method:"DELETE"});load();stats()}})}
f.onsubmit=async e=>{e.preventDefault();m.textContent="Uploading...";let r=await fetch("/api/admin/songs",{method:"POST",body:new FormData(f)}),x=await r.json();if(!r.ok){m.textContent=x.error;m.style.color="#ff7777"}else{m.textContent="Uploaded successfully";m.style.color="#a8ff2a";f.reset();load();stats()}};document.querySelector("#refresh").onclick=()=>{load();stats()};load();stats();